#include <stdio.h>

/**
 * main - a program that prints a text.
 *
 * Return: 0 if sucessesful else another number.
 */

int main(void)
{
	char tp[] = "\"Programming is like building a multilingual puzzle";

	puts(tp);
	return (0);
}
