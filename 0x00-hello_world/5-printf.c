#include <stdio.h>

/**
 * main - a program that prints a string.
 *
 * Return: 0 if successful else another number.
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
